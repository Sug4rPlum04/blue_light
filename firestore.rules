rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Locked-down default. Deploy this before making repo public.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
